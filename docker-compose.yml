version: "3.9"

services:
  frontend:
    build:
      context: .
      args:
        REACT_APP_API_URL: http://74.225.228.35:8080
    container_name: importo-frontend
    ports:
      - "80:80"
    restart: always
